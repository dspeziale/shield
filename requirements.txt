# === Core runtime ===
pydantic>=2.7.0
pydantic-settings>=2.3.0
PyYAML>=6.0.1
structlog>=24.1.0
python-dotenv>=1.0.1
typer>=0.12.3

# === Packet capture ===
scapy>=2.5.0

# === Async HTTP (Hermes gateway) ===
aiohttp>=3.9.5
tenacity>=8.3.0          # retry/backoff

# === REST API ===
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
httpx>=0.27.0             # async HTTP client / test client

# === Metrics ===
prometheus-client>=0.20.0

# === Rate limiting ===
# built-in via asyncio + token-bucket custom impl

# === Testing ===
pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-cov>=5.0.0
pytest-mock>=3.14.0
freezegun>=1.5.1
